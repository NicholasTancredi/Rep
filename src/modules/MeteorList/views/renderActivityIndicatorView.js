import React from 'react'
import {ActivityIndicator} from 'react-native'

export default ({
    ActivityIndicatorProps,
}) =>
    <ActivityIndicator
        size={1}
        {...ActivityIndicatorProps}
    />
